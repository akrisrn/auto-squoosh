export enum ImageType {
    png = 'png',
    jpeg = 'mozjpeg',
    webp = 'webp',
}

export enum Color {
    red = 31,
    green = 32,
    blue = 34,
    cyan = 36
}

export enum VarType {
    notEmpty,
    inListOrEmpty,
    inRangeOrEmpty,
    isList,
    isBool,
    whatever
}
